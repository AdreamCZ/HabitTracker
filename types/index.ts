// Shared TypeScript types

export type User = {
  id: string;
  name: string;
  email: string;
  createdAt: Date;
};

export type SelectOption = {
  label: string;
  value: string;
};

// Add more shared types as needed
